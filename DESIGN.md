---
name: dagashi-shopping-experience
version: 0.3.0
status: draft
updated: 2026-07-23
---

# 駄菓子 おかいもの体験アプリ DESIGN.md

この文書は、駄菓子おかいもの体験アプリの視覚・操作デザインの正本である。AI実装担当と人間のデザイナーは、画面を新規作成・修正するときに本書を参照し、色、書体、余白、部品、状態表現を統一する。

本書は[awesome-design-md](https://github.com/voltagent/awesome-design-md)で整理されているDESIGN.mdの考え方を参考に、本プロジェクト固有のデザイン言語として作成した。特定ブランドの外観を模倣するものではない。

## 1. 役割と参照順位

| 文書 | 定義するもの |
|---|---|
| `docs/dagashi-shopping-app-functional-specification.md` | 業務ルール、機能、対象範囲 |
| `docs/dagashi-shopping-app-technical-requirements.md` | 技術構成、非機能要件 |
| `docs/dagashi-shopping-app-screen-definition.md` | 画面、表示項目、操作、遷移 |
| `docs/dagashi-shopping-app-implementation-specification.md` | 型、API、データ、実装境界 |
| `docs/dagashi-shopping-app-implementation-procedure.md` | 実装順序、チケット完了条件、禁止事項 |
| `DESIGN.md` | 見た目、視覚階層、部品表現、動き、デザイン上の禁止事項 |

本書が他資料の業務ルールや画面遷移と競合する場合は、上位資料を優先する。色や余白を変更する場合は本書を更新し、画面ごとの独自値を増やさない。

## 2. Visual Theme & Atmosphere

### 2.1 デザインコンセプト

キーワードは「まちの駄菓子屋」「自分で選べた」「明るい紙もの」「小さな達成感」である。

子ども向け画面は、駄菓子の包み紙とイベントポスターが持つ「色・形・少しの不揃いさ」を積極的に使う。黄色い舞台、クリーム色の売り場、赤い主要操作、青い進行表示を明快に分け、商品カードはステッカーのような太枠とオフセット影で見せる。お菓子、星、丸の装飾は画面端に置き、商品名・価格・数量操作の可読領域へ重ねない。

アドミン画面は同じブランドの道具箱として扱う。子ども画面より中立的で密度を上げ、青紺のナビゲーションと白い表面を使う。楽しさより、保存状態、数値、警告、次の操作が一目で分かることを優先する。

### 2.2 目指す印象

- 子どもっぽくしすぎず、小学生が「自分でできる」と感じる。
- 駄菓子のカラフルさとUIの色を、黄・赤・青・クリームの4役へ整理して共存させる。
- イベント会場へ入った瞬間のような「ワクワク」を、太い文字、ステッカー形状、弾む影で伝える。
- 成功だけを祝うのではなく、挑戦そのものを肯定する。
- 現金を扱う場面では、遊びとスタッフ操作を明確に分ける。
- デジタルゲームではなく、実物の商品と物理スタンプカードを主役にする。

### 2.3 視覚モチーフ

- ステッカーのような太枠カードと値札
- 小さな丸スタンプ
- キャンディー、紙吹雪、星、丸、短い放射線
- 駄菓子屋の値札を思わせる太い数字
- 少し回転したカードと不揃いな楕円・雲形

モチーフは画面端へ散らし、中央の操作領域には重ねない。開始、チャレンジ結果、完了では強く、商品選択・支払いでは密度を落とす。

## 3. Color Palette & Roles

### 3.1 基本色

| Token | Hex | 役割 |
|---|---|---|
| `color.child.canvas` | `#FFCA2C` | 子ども画面のイベント感ある黄色い舞台 |
| `color.surface` | `#FFFFFF` | 商品カード、入力面 |
| `color.surface.cream` | `#FFFAF0` | 雲形の売り場、結果ポップ |
| `color.surface.sunken` | `#E6DFE1` | 無効領域 |
| `color.ink` | `#4A2333` | 太枠、見出し、基本文字 |
| `color.ink.muted` | `#674A55` | 補足、メタ情報 |
| `color.border` | `#4A2333` | 子ども画面のステッカー枠 |
| `color.primary` | `#E52335` | 主要CTA、価格、スタンプ、ストップ |
| `color.primary.hover` | `#C81429` | 主要操作のhover・active |
| `color.primary.soft` | `#FFE3E7` | 選択状態、価格の薄い背景 |
| `color.accent.blue` | `#04A9E8` | 工程表示、かごバー、スタート |
| `color.accent.blue.dark` | `#0877AD` | 青面の影、リンク |
| `color.accent.blue.soft` | `#DFF5FF` | 情報領域、商品背景 |
| `color.sun` | `#FFCA2C` | 背景、星、紙吹雪 |
| `color.sun.soft` | `#FFF3BC` | 商品画像領域 |
| `color.danger` | `#B3261E` | 取消、重大エラーだけに使用 |
| `color.danger.soft` | `#FCE8E6` | エラー背景 |
| `color.focus` | `#5B37D1` | キーボードフォーカスリング |

### 3.2 アドミン専用色

| Token | Hex | 役割 |
|---|---|---|
| `color.admin.canvas` | `#F4F6F8` | 管理画面背景 |
| `color.admin.nav` | `#1D2940` | サイドナビゲーション |
| `color.admin.nav.active` | `#2E4063` | 選択中メニュー |
| `color.admin.ink` | `#172033` | 管理画面本文 |
| `color.admin.primary` | `#2457C5` | 管理画面の主要操作 |
| `color.admin.primary.hover` | `#19449F` | hover・active |
| `color.admin.border` | `#D8DEE8` | テーブル・入力境界線 |

### 3.3 カテゴリ補助色

カテゴリ色は分類を楽しく見せるための補助であり、意味を色だけに依存しない。カテゴリ名を必ず併記する。

| Token | Background | Text |
|---|---|---|
| `category.red` | `#FBE5DE` | `#7C2D1D` |
| `category.yellow` | `#FFF1C2` | `#684A00` |
| `category.green` | `#DDF2E8` | `#104C37` |
| `category.blue` | `#E2ECFA` | `#173F77` |
| `category.purple` | `#EEE8FA` | `#4E3578` |

### 3.4 色の使用規則

- 1画面の主要CTAは原則1つだけ赤の`color.primary`にする。
- 青は進行状況、スタート、固定かごバーへ使用し、赤いCTAと役割を競合させない。
- 黄色の上に白文字を置かない。文字は`color.ink`または指定の濃色を使う。
- 成功・不成功・警告は、色だけでなくアイコン、見出し、本文で区別する。
- `color.danger`は取消、保存不能、削除相当の操作だけに使う。「ナイスチャレンジ」には使わない。
- 子ども画面のポップブルーとアドミン青紺は別Tokenとして扱う。
- ダークモードはMVP対象外。OS設定によって自動反転しない。

### 3.5 CSS変数の基準

```css
:root {
  --child-canvas: #ffca2c;
  --surface: #ffffff;
  --surface-cream: #fffaf0;
  --surface-sunken: #e6dfe1;
  --ink: #4a2333;
  --ink-muted: #674a55;
  --border: #4a2333;
  --primary: #e52335;
  --primary-hover: #c81429;
  --primary-soft: #ffe3e7;
  --pop-blue: #04a9e8;
  --pop-blue-dark: #0877ad;
  --pop-blue-soft: #dff5ff;
  --sun: #ffca2c;
  --sun-soft: #fff3bc;
  --info: #205aa7;
  --info-soft: #e2ecfa;
  --danger: #b3261e;
  --danger-soft: #fce8e6;
  --focus: #5b37d1;
  --admin-canvas: #f4f6f8;
  --admin-nav: #1d2940;
  --admin-ink: #172033;
  --admin-primary: #2457c5;
  --admin-border: #d8dee8;
}
```

## 4. Typography Rules

### 4.1 Font family

日本語は可読性を優先し、次の順で使用する。

```css
font-family:
  "Noto Sans JP",
  "Hiragino Sans",
  "Yu Gothic",
  "Meiryo",
  system-ui,
  sans-serif;
```

`Noto Sans JP`を使う場合はビルド時に取得・同梱し、店舗利用時の外部フォント読込へ依存しない。ロゴ以外で手書き風・丸文字フォントを本文に使用しない。

### 4.2 Type scale

| Token | Size / Line height | Weight | 用途 |
|---|---|---:|---|
| `type.display` | `48px / 1.15` | 800 | 開始・完了の大見出し |
| `type.challenge` | `96px / 1.00` | 800 | 結果時間。等幅数字を指定 |
| `type.child.h1` | `36px / 1.30` | 800 | 子ども画面のページ見出し |
| `type.child.h2` | `28px / 1.35` | 700 | セクション、合計見出し |
| `type.admin.h1` | `28px / 1.35` | 700 | 管理画面ページ見出し |
| `type.admin.h2` | `22px / 1.40` | 700 | 管理画面セクション |
| `type.price.lg` | `32px / 1.10` | 800 | 合計金額 |
| `type.price.md` | `22px / 1.20` | 700 | 商品価格、小計 |
| `type.body.lg` | `20px / 1.60` | 500 | 子ども向け説明 |
| `type.body` | `16px / 1.60` | 400 | 標準本文 |
| `type.label` | `16px / 1.40` | 700 | ボタン、入力ラベル |
| `type.caption` | `14px / 1.50` | 500 | 補足、日時、ステータス |
| `type.micro` | `12px / 1.40` | 600 | 管理テーブル補足だけ |

### 4.3 数字と金額

- 金額は桁区切りを使い、`1,230円`のように表示する。
- 金額、数量、結果時間は`font-variant-numeric: tabular-nums`を使用する。
- 「円」を数字より極端に小さくしない。最低16pxを確保する。
- 10秒チャレンジの判定結果は小数第2位まで表示する。
- 計測中は0.00〜4.99秒だけ小数第2位まで表示し、5.00秒以降は数字を隠してストップ操作へ集中させる。

### 4.4 文体

子ども向け文言：

- 一文を短くし、基本的に一つの行動だけを伝える。
- 難しい漢字を避けるか、ひらがなを併記する。
- 「失敗」「ざんねん」「失格」「まちがい」を結果見出しに使わない。
- 命令調より「〜してね」「〜しよう」を使う。
- エラー時は「お店の人をよんでね」と次の行動を示す。

アドミン向け文言：

- 保存済み、未保存、確認不能を明確に区別する。
- 「エラーが発生しました」だけで終わらせない。
- 取消、設定変更、商品公開では対象と結果を具体的に書く。

## 5. Spacing, Shape & Grid

### 5.1 Spacing scale

| Token | Value | 用途 |
|---|---:|---|
| `space.1` | `4px` | アイコン内部、最小差分 |
| `space.2` | `8px` | 密接な要素 |
| `space.3` | `12px` | ラベルと値 |
| `space.4` | `16px` | カード内部の最小余白 |
| `space.5` | `24px` | 標準カード余白 |
| `space.6` | `32px` | ブロック間 |
| `space.7` | `40px` | 大きな操作間 |
| `space.8` | `48px` | セクション間 |
| `space.9` | `64px` | ページ上部・主要セクション |
| `space.10` | `80px` | 開始・完了画面の余白 |

4pxグリッドを基準にする。画面固有の端数値を追加せず、既存Tokenで組み立てる。

### 5.2 Radius scale

| Token | Value | 用途 |
|---|---:|---|
| `radius.sm` | `8px` | 管理テーブル内、タグ |
| `radius.md` | `12px` | 入力、管理カード |
| `radius.lg` | `18px` | 子ども向けボタン、商品カード |
| `radius.xl` | `28px` | 結果カード、開始画面パネル |
| `radius.full` | `9999px` | スタンプ、数量ボタン、チップ |

子ども画面では`radius.lg`以上を主体にし、アドミン画面では`radius.sm`〜`md`を主体にする。角丸を入れ子にする場合、内側は外側より6px以上小さくする。

### 5.3 Content width

| 画面 | 最大幅 | 左右余白 |
|---|---:|---:|
| 子ども標準 | `1200px` | 最低`32px` |
| 開始・完了 | `960px` | 最低`48px` |
| チャレンジ | `1100px` | 最低`48px` |
| アドミン本文 | `1440px` | `24px`〜`40px` |

基準画面は1,280 × 800px、最低幅は1,024pxとする。主要操作は初期表示内に置き、商品一覧と長い管理テーブルだけをスクロール対象にする。

## 6. Depth & Elevation

### 6.1 Shadow tokens

| Token | Value | 用途 |
|---|---|---|
| `shadow.none` | `none` | 通常の管理テーブル |
| `shadow.card` | `0 2px 8px rgba(37, 35, 31, 0.08)` | 商品カード、通常カード |
| `shadow.raised` | `0 8px 24px rgba(37, 35, 31, 0.14)` | 固定かごバー、ポップオーバー |
| `shadow.modal` | `0 20px 60px rgba(23, 32, 51, 0.24)` | 結果ポップ、確認ダイアログ |

影は4段階以上に増やさない。通常状態で浮かせすぎず、固定・重なり・モーダルの順に奥行きを強くする。枠線を使える場所では影より枠線を優先する。

### 6.2 Overlay

- モーダル背景は`rgba(23, 32, 51, 0.56)`とする。
- 結果ポップだけは温かさを残すため`rgba(37, 35, 31, 0.62)`も使用できる。
- 背景操作を完全に停止し、フォーカスをダイアログ内へ閉じ込める。
- 背景ぼかしは最大4px。低性能端末で無効でも成立させる。

## 7. Component Stylings

### 7.1 Child primary button

| 項目 | 定義 |
|---|---|
| 高さ | 64px以上 |
| 横幅 | 主要CTAは240px以上、必要に応じて全幅 |
| 背景 | `color.primary` |
| 文字 | 白、`type.label`、必要なら18px |
| 角丸 | `radius.lg` |
| hover | `color.primary.hover`、上方向へ1px |
| active | 移動なし、影を弱める |
| focus | 3pxの`color.focus`リング＋2pxの外側余白 |
| disabled | `surface.sunken`背景、`ink.muted`文字、影なし |
| loading | 文言を保ち、右側にスピナー。幅を変えない |

ボタンに装飾アイコンだけを置かない。進行操作には「かごをみる」「これで買う」のような動詞を必ず表示する。

### 7.2 Child secondary button

- 高さ56px以上。
- 白背景、`color.border.strong`の2px枠、`color.ink`文字。
- 戻る・選び直す・キャンセルに使用する。
- 主要CTAと同じ強さに見せない。
- 破壊的操作へは使用せず、確認ダイアログ内で`danger`を使う。

### 7.3 Product card

| 項目 | 定義 |
|---|---|
| 表面 | 白。商品ごとに画像領域だけ薄い黄・青・赤・緑を巡回 |
| 枠 | 3px `color.ink` |
| 角丸 | 24px。不揃いな内側画像角丸を許可 |
| 内側余白 | 16px |
| 商品ビジュアル | 1:1、カード幅いっぱい、白または薄い紙色背景。有効なDrive画像は`object-fit: contain`、未設定・読込失敗時は商品別絵文字を中央表示 |
| 商品名 | 20px / 700、最大2行だが省略しない |
| 価格 | 22px / 800、数字を主役にする |
| 数量領域 | 下端へ揃え、カード間で位置を統一 |

カードは並び順に応じて±1度程度だけ回転させ、hover時に水平へ戻す。数量0は白、数量1以上は薄い黄色背景、赤枠・赤いオフセット影、小さな「かごに入っています」ラベルで示す。色だけで選択状態を示さない。

hover可能な端末ではカード全体を2pxだけ持ち上げる。タッチ操作ではhoverに依存しない。商品画像読込失敗時もカードは残し、同じ1:1領域へ`fallback_emoji`を72〜96pxで中央表示する。レイアウトを動かさない。

### 7.4 Quantity stepper

- マイナス、数量、プラスを横一列にする。
- 丸ボタンは48 × 48px以上。
- プラスは赤、マイナスは青。どちらも濃色3px枠＋押せることが分かる下影を付ける。
- 記号だけでなく`aria-label`に商品名と操作を含める。
- 数量は24px / 800、最低48px幅、中央揃え。
- 0でマイナスを無効化し、上限でプラスを無効化する。

### 7.5 Sticky cart bar

- 商品選択画面の下部へ固定する。
- 青背景、濃色4px上枠、白い上側アクセント、高さ104〜112px。
- 左に「かご ○こ」、中央に合計、右に主要CTAを配置する。
- かご0個ではCTAを無効化するが、合計0円は表示する。
- コンテンツ最下部へバーの高さ分の余白を確保する。

### 7.6 Step indicator

- 「えらぶ」「かご」「おかいけい」「チャレンジ」の4工程。
- 白い横長ピル内に置き、現在工程は青丸＋太字、完了工程は赤いチェック丸＋実線で表す。
- 未到達工程は枠線丸＋薄い文字。
- 色だけで状態を表さない。
- 子ども向け画面でのみ使用し、アドミン画面へ持ち込まない。

### 7.7 Staff panel

支払い画面内のスタッフ領域は、子ども向け領域と明確に分離する。

- 背景は`color.info.soft`、枠は`color.info`。
- 見出しに鍵アイコンと「お店の人のそうさ」を表示する。
- 子ども向けCTAから最低32px離す。
- PIN入力中も商品明細と合計を隠さない。
- 保存中、保存済み、確認不能をアイコン＋ラベルで示す。

### 7.8 Challenge start and stop

| 状態 | 表現 |
|---|---|
| Ready | 中央に青の「スタート」、説明は「10秒だと思ったらストップ！」 |
| Starting | 画面は即座にRunningへ進み、開始状態を裏側で保存 |
| Running（0.00〜4.99秒） | 中央に赤の「ストップ」。経過数字を小数第2位まで表示 |
| Running（5.00秒以降） | ストップの位置は変えず、経過数字だけを非表示 |
| Saving | ストップ直後に結果ポップを表示し、ポップ内で「結果をきろくしています…」 |
| Finished | 保存完了後に結果ポップの「つぎへ」を有効化 |

スタート・ストップボタンは最小180 × 180pxの円、または幅320px・高さ120pxの大きな角丸とする。基準画面では円を優先する。開始後に位置を変えず、誤タップを防ぐため周囲に48px以上の空白を取る。

### 7.9 Result popup

| 要素 | 定義 |
|---|---|
| コンテナ | 最大760px、クリーム、濃色5px枠、不揃いな38〜58px角丸、青いオフセット影 |
| 結果画像 | 上部中央、240〜320px、状態別に必須 |
| 見出し | 36〜48px / 800、肯定的な短文 |
| 時間 | 64〜96px、等幅数字 |
| スタンプ | 朱色の丸スタンプ＋「今回 ○こ」 |
| CTA | 下部に「つぎへ」、赤の主要ボタン。保存中は無効 |

成功時は星と紙吹雪、不成功時は明るい応援イラスト、スキップ時は買い物完了イラスト、中断時は落ち着いたスタッフ案内を使う。赤い×、泣き顔、順位、他の子の記録を使わない。

ポップは「つぎへ」まで閉じない。右上の×、背景クリック、Escapeだけで閉じる操作を提供しない。Escapeを押した場合も閉じず、CTAへフォーカスする。

### 7.10 Stamp display

- 物理スタンプを想起させる朱色の円形を使う。
- 円の中に星またはアプリ固有マークを置き、外側に「スタンプ」を併記する。
- 今回の獲得数1または2だけを表示する。
- 累積数、残り数、デジタルカード番号を表示しない。
- 15個交換の説明はテキストで示し、デジタル残高のように見せない。

### 7.11 Form controls

| 項目 | 子ども・スタッフ | アドミン |
|---|---:|---:|
| 最低高さ | 56px | 44px |
| 角丸 | 12px | 8px |
| 通常枠 | 2px | 1px |
| ラベル | 常時表示 | 常時表示 |
| エラー | 枠＋アイコン＋本文 | 枠＋アイコン＋本文 |

placeholderだけをラベルとして使わない。エラーは入力の直後へ置き、修正方法を書く。PINは数字マスク表示とし、桁数を画面外へ読み上げられるようにする。

### 7.12 Admin navigation

- 幅224pxの左サイドバー。
- 背景は`color.admin.nav`、文字は白または薄い青灰色。
- 選択中は背景色、左4pxの青線、太字の3要素で示す。
- 下部に接続状態、アプリバージョン、ログアウトを置く。
- 子ども向け画面へ戻る操作をナビゲーション上部へ固定する。

### 7.13 Admin cards and KPI

- KPIカードは白、1px枠、`radius.md`、影なし。
- ラベル14px、値32px、補足14px。
- 1カードに1指標だけ置く。
- 売上額、取引件数、販売個数、取消件数を同じ重みで並べる。
- 上昇・下降の色表現は不要。MVPでは比較指標を自動評価しない。

### 7.14 Admin data table

- ヘッダーを固定し、行高48〜56px。
- 数値は右揃え、名称は左揃え、状態はバッジにする。
- 横罫線を主体にし、全セルの縦罫線は使わない。
- hover行は薄い青灰色。選択状態はチェックまたは左線を併記する。
- 空状態では空白表だけを出さず、理由と次操作を表示する。
- 取消済み行は削除せず、`取消済み`バッジと打消しに頼らない薄い背景で区別する。

### 7.15 Status badges

| 状態 | 背景 | 文字・アイコン |
|---|---|---|
| 販売中・完了 | `primary.soft` | 濃緑＋チェック |
| 下書き・未開始 | `surface.sunken` | 濃灰＋丸 |
| 売切 | `sun.soft` | 濃茶＋箱 |
| 非表示 | `info.soft` | 濃青＋目に斜線 |
| 取消・重大エラー | `danger.soft` | 濃赤＋警告 |
| 書込中 | `info.soft` | 濃青＋スピナー |

バッジの文言を省略しない。色だけの丸印は禁止する。

### 7.16 Toast and inline feedback

- 保存成功は右上Toast、4〜6秒表示。重要な結果は画面内にも残す。
- 売上結果不明、接続不能、取消はToastだけで通知しない。
- 子ども画面でToastを多用せず、現在の操作の近くへ案内を出す。
- Toastは最大3件。新しいものを上へ積む。
- 自動消去するメッセージもスクリーンリーダーへ通知する。

## 8. Image & Illustration Direction

### 8.1 商品画像とフォールバック絵文字

- 1:1、推奨800 × 800px、WebP・PNG・JPEG、500KB以下。
- 商品全体が切れず、中央に収まる。
- 背景は白または薄い黄・青・赤・緑の単色。複数商品のコラージュにしない。
- 値段、売切、キャンペーン文字を画像内へ焼き込まない。
- 商品名を`alt`へ設定し、装飾説明を追加しない。
- Drive画像が有効なら画像を優先し、未設定・読込失敗時はSpreadsheetの`fallback_emoji`を同じ領域へ表示する。
- フォールバック絵文字は商品を表す1つの絵文字とし、72〜96pxを目安に中央配置する。背景、カード高、商品名、価格は画像表示時と揃える。
- 絵文字には商品名のアクセシブル名を設定する。同じ商品名が直後に読まれる構造では、重複読み上げを避ける実装にしてよい。
- 画像異常はアドミン画面へ警告するが、子ども画面の商品カードは除外しない。

### 8.2 結果イラスト

必要な画像アセットは次の4種類である。

| Asset | 方向性 |
|---|---|
| `challenge-success` | 星、紙吹雪、上向きの動き、笑顔 |
| `challenge-try` | 拍手、親指、やさしい放射線、挑戦を肯定 |
| `challenge-skip` | かごとスタンプ、落ち着いた完了感 |
| `challenge-help` | スタッフを呼ぶことが分かる案内、警怖感を出さない |

線は太め、面はフラット、色数は基本色＋補助2色までとする。既存キャラクターや有名菓子パッケージを模倣しない。人物を使う場合は年齢、性別、肌色を固定しすぎない抽象表現を優先する。

### 8.3 アイコン

- 2px前後の丸みのある線アイコンへ統一する。
- 16px、20px、24px、32pxの4サイズを基本とする。
- ボタン内は20〜24px、管理ナビは20px、結果は32px。
- 操作アイコンを絵文字だけにしない。画面端の装飾キャンディーと`fallback_emoji`の商品ビジュアル用途は例外とする。
- アイコンだけの操作には可視Tooltipと`aria-label`を付ける。

## 9. Motion & Feedback

### 9.1 Motion tokens

| Token | Duration | Easing | 用途 |
|---|---:|---|---|
| `motion.fast` | `120ms` | `ease-out` | hover、押下 |
| `motion.base` | `200ms` | `ease-out` | カード、Toast |
| `motion.slow` | `320ms` | `cubic-bezier(.2,.8,.2,1)` | モーダル、画面内切替 |
| `motion.celebrate` | `1200ms`まで | custom | 紙吹雪、星 |

### 9.2 Motion rules

- ボタン押下は縮小より、影と色の変化を主体にする。
- 主役カードとキャンディー装飾は8〜12px以内でゆっくり浮遊させてよい。
- 商品をかごへ入れた時はカード境界と数量を200msだけ強調する。
- 画面遷移で全面スライドを連発しない。
- 結果演出は3秒以内に収め、CTAの出現を待たせない。
- 連続点滅、激しい拡大縮小、自動再生音を使用しない。
- `prefers-reduced-motion: reduce`では紙吹雪を静止した星・丸へ置き換え、移動アニメーションを無効化する。

### 9.3 Sound

MVPは音なしを標準とする。音を追加する場合も、ミュート状態で意味が完全に伝わること、スタッフが常時OFFにできることを必須とする。不成功時のブザー音は禁止する。

## 10. Layout Principles by Screen

### 10.1 CH-01 開始

- 中央寄せの単一カラム。
- 上からロゴ、イラスト、見出し、主要CTAの順。
- 主要CTAは初期表示内に置く。
- 「スタッフ・アドミン」は右上または下端へ低強調で置く。
- 背景装飾は四隅だけに置き、CTAの周囲を空ける。

### 10.2 CH-02 商品選択

- ヘッダーと工程表示を上部固定にしない。商品領域を優先する。
- 商品カードは基準幅で4列、1,024px付近で3列。
- カテゴリ絞り込みを追加する場合は横並びチップとし、MVP必須にはしない。
- 下部のかごバーだけを固定する。
- 商品カードの高さを揃え、数量ボタンの位置をずらさない。

### 10.3 CH-03 かご・合計

- 左2/3に明細、右1/3に合計カードを置く。
- 合計カードはスクロール追従できるが、画面下部を覆わない。
- 各明細に画像、商品名、単価、数量、小計を同じ順で表示する。
- 「えらびなおす」は左、「これで買う」は右へ置く。

### 10.4 ST-01 支払い

- 左側を子ども向け案内、右側をスタッフ用会計パネルに分ける。
- 合計金額を両領域から確認できる位置に置く。
- スタッフ確定は青系パネル内に閉じ、子ども用緑CTAと混同させない。
- 保存中は画面遷移せず、結果確定後だけチャレンジへ進む。

### 10.5 CH-04 チャレンジ

- 一画面一操作。中央ボタン以外の要素を弱める。
- ReadyとRunningでボタン中心位置を変えない。
- Running中は数字のカウントアップを表示しない。
- スキップは画面下端のスタッフ領域へ低強調で置く。
- 結果は同一画面上の全画面オーバーレイとして表示する。

### 10.6 CH-05 完了

- 「おかいもの かんりょう！」と今回のスタンプ数を最優先にする。
- 物理カードをスタッフへ渡す案内をイラストと文で示す。
- 15個交換の説明は補足カードへ置く。
- 「おわる」を押すまで次の買い物を始めない。

### 10.7 Admin screens

- 左サイドバー＋上部ページヘッダー＋本文の3領域。
- ページタイトル、主要操作、フィルター、結果の順に並べる。
- 商品編集は一覧と編集パネルの2カラムまたは明示的な詳細ページとする。
- 取消は通常編集フォームへ混ぜず、専用確認ダイアログまたは専用URLで扱う。
- 接続異常と不完全書込はダッシュボード上部へ常設警告として表示する。

## 11. Responsive Behavior

MVPの正式対応幅は1,024px以上であり、スマートフォン向けUIは作らない。

| 幅 | 子ども画面 | アドミン画面 |
|---|---|---|
| `>= 1280px` | 商品4列、標準余白 | 224pxサイドバー、広い表 |
| `1024–1279px` | 商品3列、余白32px | 200pxサイドバー、表は必要箇所だけ横スクロール |
| `< 1024px` | 主要操作を壊さず縮小し、スタッフへ推奨幅を案内 | 原則非対応案内。データ変更を無理に行わせない |

### 11.1 縮小規則

- 文字を14px未満へ縮小して収めない。
- タッチ領域を44px未満へ縮小しない。子ども向けは48px以上を維持する。
- 商品画像と商品名を隠して列数を増やさない。
- 合計金額と主要CTAは折返しても同じ視覚グループに保つ。
- アドミン表の重要列は商品名、個数、金額、状態、操作の順に残す。
- 125%表示倍率でも主要操作と保存状態を確認できるようにする。

## 12. Accessibility

### 12.1 必須基準

- 通常文字と背景はWCAG AA相当のコントラストを確保する。
- 主要操作、入力、カード内ボタンへ視認可能なフォーカスリングを出す。
- キーボードだけで開始、商品選択、会計補助、管理操作を完了できる。
- 見出し順序を飛ばさない。
- ダイアログは`role=dialog`、結果通知は内容に応じて適切なlive regionを使う。
- 保存中・エラー・完了をスクリーンリーダーへ通知する。
- 装飾画像は空の`alt`、商品画像は商品名、結果画像は本文と重複しすぎない短い説明にする。
- ボタン名だけで操作結果が分かるようにする。
- 色、音、動きだけで意味を伝えない。

### 12.2 Focus order

視覚順とDOM順を一致させる。商品カードでは商品名、価格、マイナス、数量、プラスの順とする。固定かごバーは商品一覧の後にフォーカスされるようDOMを配置し、CSSだけで先頭へ移動させない。

### 12.3 Touch and error prevention

- 子ども向け主要操作は48 × 48px以上、隣接操作間は8px以上。
- 売上確定、取消、買い物中止は確認段階を持つ。
- 非同期ボタンは処理中の二重押下を防ぐ。
- エラー後もかごと入力を保持する。
- タイムアウトする操作には、残り時間を読む必要がない設計を優先する。

## 13. Do's and Don'ts

### 13.1 Do

- Drive画像または商品別絵文字、商品名、価格を常に一緒に見せる。
- 一画面の主要CTAを一つに絞る。
- 選択状態を枠、ラベル、数量で示す。
- 支払い操作をスタッフ領域として視覚分離する。
- 成功・不成功のどちらにも画像と肯定的な文言を用意する。
- 保存結果と次の操作を画面内に残す。
- 共通Tokenと部品を使い、画面間の数値を揃える。
- 実物の駄菓子と物理スタンプカードを主役にする。

### 13.2 Don't

- 虹色グラデーションを画面背景や全ボタンへ使わない。色面はフラットに保つ。
- 黄・赤・青・クリームの役割を崩し、同じ意味へ複数色を割り当てない。
- 薄い汎用カード影へ戻さない。子ども画面は太枠＋方向を揃えたオフセット影を使う。
- 小さな文字、細い文字、長文で子どもへ説明しない。
- hoverしないと操作が分からないUIを作らない。
- 絵文字をUIアイコンや結果画像の代用品にしない。商品別の`fallback_emoji`だけは許可する。
- 赤い×、悲しい顔、ブザー、順位表を使わない。
- スタンプ累積数や個人情報を画面へ追加しない。
- 子ども画面へ管理用の内部ID、APIエラー、技術用語を表示しない。
- Drive画像エラーだけを理由に商品カードを消さない。商品別の`fallback_emoji`へ切り替える。
- アドミン画面を子ども向けの大きな装飾で埋めない。
- 画面ごとに独自の色、余白、角丸、影を作らない。

## 14. Design Acceptance Checklist

### 14.1 共通

- [ ] 本書の色、書体、余白、角丸、影Tokenを使用している。
- [ ] 主要CTAが一画面に複数競合していない。
- [ ] loading、empty、error、success、disabledの各状態がある。
- [ ] 色だけで状態を伝えていない。
- [ ] 1,024px、1,280px、125%表示倍率で確認した。
- [ ] キーボードフォーカスが見える。
- [ ] `prefers-reduced-motion`へ対応している。

### 14.2 子ども画面

- [ ] Drive画像または商品別絵文字、商品名、価格が同時に見える。
- [ ] Drive画像の有無・読込結果で商品カードのサイズや配置が変わらない。
- [ ] 4.99秒まで経過数字が見え、5.00秒以降はストップまで数字が見えない。
- [ ] タッチ対象が48px以上ある。
- [ ] 合計金額と次の操作が見つけやすい。
- [ ] 難しい漢字、否定的な結果表現、技術用語を避けている。
- [ ] 支払いのスタッフ操作が子ども操作と混同しない。
- [ ] 結果ポップに状態別画像、時間、文言、今回のスタンプ数がある。

### 14.3 アドミン画面

- [ ] 売上保存状態、取消状態、接続状態がラベル付きで分かる。
- [ ] 数値列が右揃えで比較しやすい。
- [ ] 取消等の破壊的操作に対象・理由・確認がある。
- [ ] 空状態とエラー状態に次の操作がある。
- [ ] 子どもの氏名、顔、カード番号、スタンプ残高を表示していない。

## 15. Agent Prompt Guide

### 15.1 実装担当エージェントへの基本指示

```text
このプロジェクトのUIを実装するときは、ルートのDESIGN.mdを視覚仕様の正本として使用する。
画面の機能・遷移はdocs/dagashi-shopping-app-screen-definition.mdに従う。
既存Tokenにない色、余白、角丸、影を画面ごとに追加しない。
子ども画面は黄色い舞台、クリーム色の売り場、白いステッカー商品カード、赤い主要CTA、青い進行表示を中心にする。
商品カードは有効なDrive画像を優先し、未設定・読込失敗時は商品別のfallback_emojiを同じ領域へ表示する。
10秒チャレンジは4.99秒まで時間を表示し、5.00秒以降は結果確定まで数字だけを隠す。
アドミン画面は青紺のナビゲーションと中立的な白・灰色の表面を使う。
成功・不成功を色だけで区別せず、画像、見出し、本文、スタンプ数を表示する。
1,024px以上、キーボード操作、prefers-reduced-motionを必ず確認する。
```

### 15.2 子ども画面生成用の短縮指示

```text
Pop-up dagashi market experience, not a generic dashboard or mobile game.
Use #FFCA2C stage canvas, #FFFAF0 cloud-like shopping surfaces, white sticker cards,
#E52335 primary actions and #04A9E8 progress/ribbon accents.
Use thick #4A2333 outlines, consistent offset shadows, bold rounded Japanese type,
48px minimum touch targets, one dominant action per screen, and candy/star decorations around—not over—content.
Prefer valid Drive product photos; use the product-specific fallback emoji in the same visual area when absent. Keep physical stamp cards as protagonists.
```

### 15.3 アドミン画面生成用の短縮指示

```text
Build a calm operational dashboard within the same dagashi brand.
Use #F4F6F8 canvas, #1D2940 navigation, white bordered panels,
compact 8–12px corners, right-aligned numeric columns, explicit status labels,
clear save and error states, and no decorative child-facing celebration motifs.
```

## 16. 未決定アセットと変更ルール

| ID | 未決定事項 | 現在の扱い |
|---|---|---|
| DS-HC-001 | 正式なアプリ名・ロゴ | テキストロゴで仮実装 |
| DS-HC-002 | マスコット | なくても成立する構成にする |
| DS-HC-003 | 結果イラスト4種 | 方向性だけ本書で固定 |
| DS-HC-004 | 朱色スタンプの固有マーク | 星または仮マーク |
| DS-HC-005 | ブランドカラーの最終承認 | 本書の色をv0.1仮決定として実装可能 |
| DS-HC-006 | 音 | MVPは音なし |

上記の未決定アセットが確定しても、商品選択、会計、チャレンジ、管理画面の構造は変更しない。正式ロゴや画像を入れた結果、コントラストや文字の可読性が下がる場合は、アセット側または周囲の表面色を調整して基準を維持する。

## 17. Reference

- [VoltAgent / awesome-design-md](https://github.com/voltagent/awesome-design-md)
- [画面定義書](./docs/dagashi-shopping-app-screen-definition.md)
- [実装定義書](./docs/dagashi-shopping-app-implementation-specification.md)
- [機能・運用仕様書](./docs/dagashi-shopping-app-functional-specification.md)
- [技術要件定義書](./docs/dagashi-shopping-app-technical-requirements.md)
